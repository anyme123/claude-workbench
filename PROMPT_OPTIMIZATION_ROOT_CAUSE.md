# 提示词优化功能崩溃 - 根本原因分析

## 📊 诊断结果

**状态：** 后端修复已实现并编译，但前端配置仍然存在问题

### ✅ 已实现的修复（11月11日）

1. **后端长度限制** (`src-tauri/src/commands/claude.rs`)
   - ✅ MAX_PROMPT_LENGTH: 50,000 字符
   - ✅ MAX_CONTEXT_LENGTH: 30,000 字符
   - ✅ MAX_TOTAL_LENGTH: 100,000 字符
   - ✅ 智能截断逻辑
   - ✅ 详细错误处理

2. **编译状态**
   - ✅ claude.rs 最后修改：11月11日 23:34
   - ✅ claude-workbench.exe 编译：11月13日 08:12
   - ✅ 修复代码已生效

### ❌ 仍然存在的问题

## 🔴 问题 1：前端默认配置过于宽松

**文件：** `src/lib/promptContextConfig.ts`

**当前配置（危险）：**
```typescript
DEFAULT_CONTEXT_CONFIG: {
  maxMessages: 15,  // 15条消息
  maxAssistantMessageLength: 2000,  // 每条助手消息最多 2000 字符
  maxUserMessageLength: 1000,  // 每条用户消息最多 1000 字符
  includeExecutionResults: true,  // 包含执行结果
  maxExecutionResultLength: 500,
}
```

**危险性计算：**
```
假设 15 条消息：7 条助手 + 7 条用户 + 1 条执行结果
- 助手消息：7 × 2000 = 14,000 字符
- 用户消息：7 × 1000 = 7,000 字符
- 执行结果：1 × 500 = 500 字符
- 分隔符和前缀：约 500 字符
= 对话上下文总计：22,000 字符

加上其他组件：
- 对话上下文：22,000 字符
- 用户提示词：可能 5,000-15,000 字符
- 项目上下文：3,000 字符
- 系统提示词：600 字符
= 总计：30,600-40,600 字符（接近安全限制）
```

**如果用户提示词较长（10,000字符+），极易触发崩溃！**

## 🔴 问题 2："详细模式"极度危险

**文件：** `src/lib/promptContextConfig.ts:74-80`

**"详细模式"配置（极度危险）：**
```typescript
detailed: {
  maxMessages: 30,  // 30条消息！
  maxAssistantMessageLength: 5000,  // 每条 5000 字符！
  maxUserMessageLength: 2000,  // 每条 2000 字符！
  includeExecutionResults: true,
  maxExecutionResultLength: 1000,
}
```

**危险性计算：**
```
假设 30 条消息：15 条助手 + 15 条用户
- 助手消息：15 × 5000 = 75,000 字符
- 用户消息：15 × 2000 = 30,000 字符
- 执行结果：若干 × 1000 = 5,000 字符（估算）
= 对话上下文总计：110,000 字符

加上其他组件：
- 对话上下文：110,000 字符
- 用户提示词：10,000 字符
- 项目上下文：3,000 字符
- 系统提示词：600 字符
= 总计：123,600 字符

**远超后端 MAX_TOTAL_LENGTH (100,000 字符) 限制！**
**必然导致崩溃！**
```

## 🔴 问题 3：前端没有总长度预检查

**文件：** `src/components/FloatingPromptInput/hooks/usePromptEnhancement.ts`

**问题代码（第 122-144 行）：**
```typescript
// 获取项目上下文
const projectContext = await getProjectContext();

// 获取对话上下文
let context = getConversationContext ? getConversationContext() : undefined;

// 合并上下文
if (projectContext) {
  context = context ? [...context, projectContext] : [projectContext];
}

// ❌ 直接调用，没有验证总长度
const result = await api.enhancePrompt(trimmedPrompt, selectedModel, context);
```

**缺失的验证：**
- ❌ 没有计算 `context` 数组的总长度
- ❌ 没有计算 `trimmedPrompt + context + projectContext` 的总长度
- ❌ 没有在发送前给用户友好的提示
- ❌ 没有智能截断上下文的逻辑

## 🔴 问题 4：后端限制仍然偏大

**文件：** `src-tauri/src/commands/claude.rs:3083-3085`

**当前限制：**
```rust
const MAX_PROMPT_LENGTH: usize = 50_000;  // 50,000 字符 ≈ 16,000 tokens
const MAX_CONTEXT_LENGTH: usize = 30_000;  // 30,000 字符 ≈ 10,000 tokens
const MAX_TOTAL_LENGTH: usize = 100_000;  // 100,000 字符 ≈ 33,000 tokens
```

**问题：**
- 100,000 字符约 33,000 tokens，对于**提示词优化**这种简单任务来说仍然过大
- Claude 模型需要为输出预留 token 空间
- 实际可用输入 token 可能更少
- 建议降低到 **40,000 字符（约 13,000 tokens）** 更安全

---

## ✅ 推荐修复方案

### 修复 1：降低前端默认配置（立即生效）

**修改文件：** `src/lib/promptContextConfig.ts:43-49`

```typescript
/**
 * 默认配置
 * ⚡ 优化：降低默认值以防止 token 过大导致崩溃
 * 预计总长度：8条 × 1500字符 ≈ 12,000字符，加上提示词和项目上下文约 20,000 字符（安全）
 */
export const DEFAULT_CONTEXT_CONFIG: PromptContextConfig = {
  maxMessages: 8,  // 从 15 降到 8
  maxAssistantMessageLength: 1000,  // 从 2000 降到 1000
  maxUserMessageLength: 500,  // 从 1000 降到 500
  includeExecutionResults: false,  // 默认关闭以减少长度
  maxExecutionResultLength: 300,  // 从 500 降到 300
};
```

**效果：**
- 对话上下文从 ~22,000 降到 ~12,000 字符
- 总输入从 ~40,000 降到 ~30,000 字符
- **立即生效，无需重新编译**

### 修复 2：调整"详细模式"配置

**修改文件：** `src/lib/promptContextConfig.ts:74-80`

```typescript
detailed: {
  name: '详细模式',
  description: '完整上下文，适合复杂任务（注意：可能接近 token 限制）',
  config: {
    maxMessages: 15,  // 从 30 降到 15
    maxAssistantMessageLength: 2000,  // 从 5000 降到 2000
    maxUserMessageLength: 1000,  // 从 2000 降到 1000
    includeExecutionResults: true,
    maxExecutionResultLength: 500,  // 从 1000 降到 500
  } as PromptContextConfig,
},
```

**效果：**
- 对话上下文从 ~110,000 降到 ~45,000 字符
- 总输入从 ~120,000 降到 ~58,000 字符
- **仍然详细，但不会崩溃**

### 修复 3：添加前端总长度验证（推荐）

**修改文件：** `src/components/FloatingPromptInput/hooks/usePromptEnhancement.ts`

**在第 122-144 行之间添加：**

```typescript
// 获取项目上下文
const projectContext = await getProjectContext();

// 获取对话上下文
let context = getConversationContext ? getConversationContext() : undefined;

// 合并上下文
if (projectContext) {
  context = context ? [...context, projectContext] : [projectContext];
}

// ⚡ 新增：验证总长度
const MAX_SAFE_TOTAL_LENGTH = 35_000; // 对应后端 40,000 限制（留些余量）
const contextTotalLength = context ? context.join('').length : 0;
const totalLength = trimmedPrompt.length + contextTotalLength;

console.log('[handleEnhancePrompt] Length check:', {
  promptLength: trimmedPrompt.length,
  contextLength: contextTotalLength,
  totalLength: totalLength,
  limit: MAX_SAFE_TOTAL_LENGTH
});

if (totalLength > MAX_SAFE_TOTAL_LENGTH) {
  // 智能截断上下文
  const availableContextLength = MAX_SAFE_TOTAL_LENGTH - trimmedPrompt.length - 1000; // 预留1000字符
  if (availableContextLength > 3000) {
    // 从后往前截取上下文（保留最近的对话）
    let truncatedContext: string[] = [];
    let currentLength = 0;

    for (let i = context.length - 1; i >= 0; i--) {
      if (currentLength + context[i].length <= availableContextLength) {
        truncatedContext.unshift(context[i]);
        currentLength += context[i].length;
      } else {
        break;
      }
    }

    console.warn('[handleEnhancePrompt] Context truncated:', {
      original: context.length,
      truncated: truncatedContext.length,
      savedChars: contextTotalLength - currentLength
    });

    context = truncatedContext;
  } else {
    // 提示词太长，无法添加上下文
    const target = isExpanded ? expandedTextareaRef.current : textareaRef.current;
    if (target) {
      updateTextareaWithUndo(target,
        trimmedPrompt + `\n\n⚠️ 提示词过长（${trimmedPrompt.length} 字符），无法添加对话上下文。建议缩短提示词或禁用项目上下文。`
      );
    }
    setIsEnhancing(false);
    return;
  }
}

// 继续调用 API
const result = await api.enhancePrompt(trimmedPrompt, selectedModel, context);
```

**效果：**
- ✅ 实时验证总长度
- ✅ 智能截断上下文（保留最近的对话）
- ✅ 友好的用户提示
- ✅ 防止崩溃

### 修复 4：降低后端限制（需要重新编译）

**修改文件：** `src-tauri/src/commands/claude.rs:3083-3085`

```rust
// ⚡ 优化：降低限制以适应提示词优化的实际需求
const MAX_PROMPT_LENGTH: usize = 20_000;  // 从 50,000 降到 20,000
const MAX_CONTEXT_LENGTH: usize = 15_000;  // 从 30,000 降到 15,000
const MAX_TOTAL_LENGTH: usize = 40_000;  // 从 100,000 降到 40,000（约 13k tokens）
```

**效果：**
- 更严格的后端保护
- 防止前端配置错误导致的崩溃
- **需要重新编译：** `cd src-tauri && cargo build --release`

---

## 📊 修复效果对比

| 场景 | 修复前（字符） | 修复后（字符） | 状态 |
|------|--------------|--------------|------|
| **默认配置** | ~40,000 | ~20,000 | ✅ 安全 |
| **详细模式** | ~120,000 | ~45,000 | ✅ 安全 |
| **最坏情况** | 无限制 | ~40,000 | ✅ 有限制 |

---

## 🚀 实施步骤

### 步骤 1：修改前端配置（立即生效）

1. 修改 `src/lib/promptContextConfig.ts`
2. 保存文件
3. **无需重新编译**，刷新前端即可生效

### 步骤 2：添加前端验证（推荐）

1. 修改 `src/components/FloatingPromptInput/hooks/usePromptEnhancement.ts`
2. 添加总长度验证和智能截断逻辑
3. 保存文件
4. **无需重新编译**，刷新前端即可生效

### 步骤 3：降低后端限制（可选）

1. 修改 `src-tauri/src/commands/claude.rs`
2. 重新编译后端：
   ```bash
   cd src-tauri
   cargo build --release
   ```
3. 重启应用

---

## 🎯 总结

### 根本原因

1. **前端配置过于宽松**，允许生成超长上下文
2. **"详细模式"极度危险**，可能生成 120,000+ 字符
3. **前端缺少总长度验证**，直接发送到后端
4. **后端限制偏大**，100,000 字符仍然可能超过模型限制

### 为什么修复后仍然崩溃

- 后端修复只能**截断**和**报错**，不能完全避免用户使用"详细模式"
- 如果用户配置使用"详细模式"，前端会生成 120,000+ 字符的上下文
- 后端收到后只能截断到 100,000 字符，但这仍然可能超过某些模型的限制
- 即使不超过，也会导致响应极慢或超时

### 推荐方案

**立即实施修复 1 和修复 2**（不需要重新编译，立即生效）
- 这将解决 90% 的崩溃问题

**可选实施修复 3**（提升用户体验）
- 添加实时验证和智能截断

**可选实施修复 4**（更严格的保护）
- 降低后端限制，但需要重新编译

---

## 📝 修复时间估算

- 修复 1 + 2（前端配置）：**2 分钟**
- 修复 3（前端验证）：**15 分钟**
- 修复 4（后端限制）：**5 分钟编码 + 3 分钟编译**
- **总计：** 25 分钟

---

## ✅ 验证方法

### 测试用例 1：默认配置
```
1. 重置配置到默认值
2. 输入 5,000 字符的提示词
3. 在有 10+ 条对话历史的情况下使用提示词优化
4. 预期：正常工作，不崩溃
```

### 测试用例 2：详细模式
```
1. 切换到"详细模式"
2. 输入 5,000 字符的提示词
3. 在有 20+ 条对话历史的情况下使用提示词优化
4. 预期：正常工作或友好提示，不崩溃
```

### 测试用例 3：超长提示词
```
1. 使用默认配置
2. 输入 20,000 字符的提示词
3. 使用提示词优化
4. 预期：前端提示"提示词过长"或智能截断，不崩溃
```

---

**分析完成时间：** 2025-11-13
**分析人员：** Droid AI (Claude Agent)
